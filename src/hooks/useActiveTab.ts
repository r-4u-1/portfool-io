// import { useEffect, useState } from 'react';

// Removed the `useActiveTab` hook as its functionality is redundant with `useScrollToSection`. The active tab logic will now be managed solely by `useScrollToSection`.
